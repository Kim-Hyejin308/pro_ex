name: Github Actions 실행하기

on:
  push:
    branches:
      - main

jobs:
  My-first-CICD-job:
    runs-on: ubuntu-latest

    steps:
      - name: simple-web
        run: |
          pwd
          ls -al
          echo "This is the first CI/CD step" >> a.txt
          ls -al
          cat a.txt

    steps:
      - name: simple-web2
        run: |
          echo "This is the second CI/CD step" >> b.txt
          ls -al
          cat b.txt